### Focus Questions  
1. Write down the commands to identify a router as CHAP1, give it a console password of cisco1, a password of cisco2 on all vty links, and a secret password of class. Ensure that all passwords are encrypted. Add a message of the day banner telling people to keep out of they are not authorised. Save the configuration.  
2. Configure the Fastethernet interface 0/1 with the address 172.30.45.1/24. Ensure that interface is brought up. How would the configuration change if the address we being applied to a DCE serial interface?  
3. How would you verify that?  
4. When a packet is sent over a path which involves multiple hops, when do the source and destination IP addresses change? If each hop is using an Ethernet channel, when do the source and destination MAC addresses change?  
5. What does a router do when a packet arrives on an interface?  
6. If a router has a choice between two routes to a destination, one which has been provided by the routing protocol OSPF and the other by the routing protocol RIP, which one will the router choose? Why?  
7. What is load balancing?  
8. How can a router learn about routes?  
9. When are directly connected routes added into the routing table?